{
  "name": "ekm-spa",
  "version": "1.0.0",
  "description": "Example SPA using Elm with elm-ui and ports",
  "scripts": {
    "start": "elm-spa server",
    "build": "elm-spa build",
    "fmt": "elm-format src/ --yes",
    "test": "elm-test"
  },
  "dependencies": {
    "elm-spa": "^6.0.4"
  },
  "devDependencies": {
    "elm-format": "^0.8.7",
    "elm-test": "0.19.1-revision15"
  }
}
